import {GridRows} from "@/components/templates/grids/grid";
import UpdateCard from "@/components/dashboard/updates/update-card";

interface SingleColumnGridProps {
    data: UpdateData[];
}

export default function SingleColumnGrid({data} : SingleColumnGridProps) {
    return (

    )
}